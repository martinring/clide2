/*package clide.core.projects

import akka.persistence.EventsourcedProcessor
import akka.actor.ActorLogging

class ProjectActor(info: ProjectInfo) extends EventsourcedProcessor with ActorLogging {
  def receiveRecover = {
    case _ =>
  }

  def receiveCommand = {
    case _ =>
  }
}*/
